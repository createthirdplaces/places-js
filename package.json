{
  "type":"module",
  "scripts": {
    "build": "rollup -c rollup.config.js",
    "updateLocal": "npm run build && yalc publish --push --replace"
  },
  "devDependencies": {
    "rollup": "^4.46.3"
  }
}
